# Dependencies
node_modules/

# Archive — local-only, not published
.archive/

# AEI raw data cache — 77-219 MB CSVs fetched by scripts/fetch-anthropic-data.ts;
# only the small transformed artifacts in src/data/anthropic/ are committed
.anthropic-cache/

# Build output
dist/

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment — NEVER commit real credentials
.env
.env.local
.env.*
!.env.example

# API key files
.bls-api-key

# Secrets and certificates
*.pem
*.key
*.p12
*.pfx
id_rsa*
credentials.json
service-account*.json
config.local.*

# Python (fetch scripts may use)
__pycache__/
.venv/
venv/
*.pyc

# Jupyter
.ipynb_checkpoints/

# Logs
*.log
npm-debug.log*

# Vite
*.local

# Test coverage
coverage/

# OS files
Thumbs.db

# Session transcripts (the finalization docket ruling): date-prefixed conversation
# captures at the repo root never ship — the class, not the instances.
/2[0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt
.onet-cache/
